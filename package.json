{
  "name": "arkode-llm-platform",
  "version": "1.0.0",
  "description": "AnythingLLM instance for Arkode, deployed on Railway.",
  "main": "server/index.js",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "setup": "npm install --prefix server --legacy-peer-deps && npm install --prefix frontend --legacy-peer-deps",
    "build": "npm run setup && cd frontend && npm run build",
    "start": "cd server && node index.js"
  },
  "keywords": [
    "anythingllm",
    "llm",
    "rag",
    "ai",
    "chatbot",
    "railway",
    "arkode"
  ],
  "author": "Arkode",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
